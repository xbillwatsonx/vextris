/**
 * Vextris — Vex Systems
 *
 * Vex mark spawning, alignment detection (immediate-consumption cluster processing,
 * full-bank early exit, visible-only scan), spell bank management,
 * Color Vex (weighted, total board), Shape Vex (uniform, total board),
 * Shadow Vex (40% threshold, visible-only, global cast sequence).
 *
 * v0.3 — scaffold only.
 */

export {};
