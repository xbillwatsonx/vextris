/**
 * Vextris — Board Model
 *
 * Board stored as board[row][col]: 22 rows × 10 columns.
 * arrayRow = y + hiddenRows (hiddenRows = 2).
 * Visible board: array rows 2..21 (logical y 0..19).
 * Hidden spawn rows: array rows 0..1 (logical y -2, -1).
 *
 * v0.3 — scaffold only.
 */

export {};
